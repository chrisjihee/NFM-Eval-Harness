# Handoff Pointer

The canonical INL handoff package is:

- **`NFM-Eval-Harness-delivery`**

Use that repository for:
- 30-minute acceptance test
- curated final results (`results/final/`)
- `docs/00`–`08` handoff guide

Use **this** repository (`NFM-Eval-Harness`) for:
- engineering source
- implementation changes
- historical decisions
- intermediate experiments
- assistant / Claude Code provenance
