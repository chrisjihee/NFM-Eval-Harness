# Results

This directory contains local engineering run artifacts. These may include smoke runs,
partial runs, legacy baseline runs, candidate runs, and pre-delivery experiments — mixed
across stages.

For curated, repeated, final handoff results use:
- `NFM-Eval-Harness-delivery/results/final/`
